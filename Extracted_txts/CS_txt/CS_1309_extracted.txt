Chapter 17: Windows Programming 577

 

)

©)
@

©)
@®

(g)

a)

Run any Windows based program and see whether you can
identify all the elements of the application window.

How would you minimize a window programmatically?

What would happen if we do not place WM_QUIT message
in the message queue when the user tries to close the window.

Explain the need of RegisterClassEx( ) function.

What is the difference between GetMessage( ) and
DispatchMessage( ) function?

Write a program, which receives an integer as a command line
argument, creates a button window, and based on the value of
the integer displays button window as maximized / minimized
/ normal.

Try to display a window with different combinations of
window styles and observer the results.
